dependencies {
	classpath group: "com.liferay", name: "com.liferay.gradle.plugins.alloy.taglib", version: "2.0.6"

	constraints {
		classpath(group: "com.liferay", name: "com.liferay.gradle.plugins.jasper.jspc") {
			version {
				strictly "2.1.0"
			}
		}
	}
}