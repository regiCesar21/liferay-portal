@default = "waitForConsoleTextPresent"
definition {

	function waitForConsoleTextPresent() {
		WaitForSPARefresh();

		selenium.waitForConsoleTextPresent(${value1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}