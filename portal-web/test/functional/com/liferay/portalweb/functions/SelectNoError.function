@default = "select"
definition {

	function select() {
		WaitForSPARefresh();

		selenium.pause(5000);

		WaitForSPARefresh();

		selenium.waitForVisible(${locator1}, ${value1});

		selenium.mouseOver(${locator1});

		selenium.select(${locator1}, ${value1});
	}

}