@default = "mouseDown"
definition {

	function mouseDown() {
		WaitForSPARefresh();

		selenium.mouseDown(${locator1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}