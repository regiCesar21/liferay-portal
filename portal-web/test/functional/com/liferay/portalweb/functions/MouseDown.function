@default = "mouseDown"
definition {

	function mouseDown(locator1) {
		WaitForSPARefresh();

		selenium.mouseDown(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}