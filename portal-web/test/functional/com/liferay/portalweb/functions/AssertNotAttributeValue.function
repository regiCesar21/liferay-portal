@default = "assertNotAttributeValue"
definition {

	function assertNotAttributeValue(locator2, locator1, value1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "false");

		selenium.waitForVisible(${locator1}, "false");

		selenium.assertNotAttributeValue(${locator1}, ${locator2}, ${value1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}