@default = "assertConsoleTextPresent"
definition {

	function assertConsoleTextPresent() {
		WaitForSPARefresh();

		selenium.assertConsoleTextPresent(${value1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}