@default = "assertTextNotPresent"
definition {

	function assertTextNotPresent() {
		WaitForSPARefresh();

		selenium.waitForTextNotPresent(${value1});

		selenium.assertTextNotPresent(${value1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}