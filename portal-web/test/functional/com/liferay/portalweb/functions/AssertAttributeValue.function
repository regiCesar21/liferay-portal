@default = "assertAttributeValue"
definition {

	function assertAttributeValue(locator1, value1, value2) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.waitForVisible(${locator1}, "true");

		selenium.assertAttributeValue(${locator1}, ${value1}, ${value2});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

	function assertAttributeValueNotVisible(locator1, value1, value2) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.assertAttributeValue(${locator1}, ${value1}, ${value2});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}