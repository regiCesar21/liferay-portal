@default = "isNotEditable"
definition {

	function isNotEditable() {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "false");

		selenium.waitForVisible(${locator1}, "false");

		selenium.isNotEditable(${locator1});
	}

}