@default = "confirm"
@summary = "Execute the confirmation of a confirmation window"
definition {

	function confirm() {
		selenium.getConfirmation(${value1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();

		WaitForSPARefresh();
	}

	function dismissAlert() {
		selenium.dismissAlert();
	}

}