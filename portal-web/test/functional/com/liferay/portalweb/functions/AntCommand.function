@default = "antCommand"
@summary = "ant -f ${locator1} ${value1}"
definition {

	function antCommand(locator1, value1) {
		selenium.antCommand(${locator1}, ${value1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}