@default = "robotType"
@summary = "Type to '${locator1}' the value '${value1}'"
definition {

	function robotType() {
		WaitForSPARefresh();

		selenium.robotType(${locator1});
	}

	function robotTypeShortcut() {
		WaitForSPARefresh();

		selenium.robotTypeShortcut(${locator1});
	}

}