@default = "sikuliMouseMove"
definition {

	function sikuliMouseMove() {
		WaitForSPARefresh();

		selenium.sikuliMouseMove(${locator1});

		selenium.pause(1000);

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}