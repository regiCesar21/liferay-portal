@default = "sikuliType"
definition {

	function sikuliType() {
		WaitForSPARefresh();

		selenium.sikuliType(${locator1}, ${value1});

		selenium.pause(2000);

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}