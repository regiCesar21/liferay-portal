@default = "assertAccessible"
definition {

	function assertAccessible(locator1) {
		WaitForSPARefresh();

		selenium.assertAccessible(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}