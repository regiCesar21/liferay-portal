@default = "selectFrame"
definition {

	function selectFrame(locator1) {
		if (contains(${locator1}, "/iframe")) {
			WaitForLiferayEvent(
				attributeName = "src",
				attributeValue = "dialog-iframe-popup",
				eventName = "modalIframeLoaded");

			selenium.mouseOver(${locator1});

			selenium.selectFrame(${locator1});

			selenium.assertJavaScriptErrors(${value1});

			selenium.assertLiferayErrors();
		}
		else {
			SelectFrame.selectFrameTop();
		}

		WaitForSPARefresh();
	}

	function selectFrameNoLoading(locator1) {
		if (contains(${locator1}, "/iframe")) {
			selenium.waitForVisible(${locator1}, ${value1});

			selenium.mouseOver(${locator1});

			selenium.selectFrame(${locator1});

			selenium.assertJavaScriptErrors(${value1});

			selenium.assertLiferayErrors();
		}
		else {
			SelectFrame.selectFrameTop();
		}

		WaitForSPARefresh();
	}

	function selectFrameTop() {
		selenium.selectFrame("relative=top");

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();

		WaitForSPARefresh();
	}

	function selectSecondFrame(locator1) {
		if (contains(${locator1}, "/iframe")) {
			selenium.waitForVisible(${locator1}, ${value1});

			selenium.mouseOver(${locator1});

			selenium.selectFrame(${locator1});

			selenium.assertJavaScriptErrors(${value1});

			selenium.assertLiferayErrors();
		}
		else {
			SelectFrame.selectFrameTop();
		}

		WaitForSPARefresh();
	}

}