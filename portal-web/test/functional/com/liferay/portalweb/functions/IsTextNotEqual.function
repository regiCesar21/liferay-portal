@default = "isTextNotEqual"
definition {

	function isNotText() {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.isNotText(${locator1}, ${value1});
	}

	function isNotValue() {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.isNotValue(${locator1}, ${value1});
	}

	function isTextNotEqual(locator1) {
		WaitForSPARefresh();

		if (contains(${locator1}, "/input")) {
			IsTextNotEqual.isNotValue();
		}
		else {
			IsTextNotEqual.isNotText();
		}
	}

}