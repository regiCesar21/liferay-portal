@default = "assertTextPresent"
definition {

	function assertTextPresent() {
		WaitForSPARefresh();

		selenium.waitForTextPresent(${value1});

		selenium.assertTextPresent(${value1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}