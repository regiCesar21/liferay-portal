@default = "isSelectedLabel"
definition {

	function isSelectedLabel() {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "false");

		selenium.isSelectedLabel(${locator1}, ${value1});
	}

}