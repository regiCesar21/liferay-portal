@default = "isSelectedLabel"
definition {

	function isSelectedLabel(locator1, value1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.isSelectedLabel(${locator1}, ${value1});
	}

}