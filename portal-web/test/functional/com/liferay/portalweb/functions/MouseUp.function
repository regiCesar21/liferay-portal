@default = "mouseUp"
definition {

	function mouseUp(locator1) {
		WaitForSPARefresh();

		selenium.mouseUp(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}