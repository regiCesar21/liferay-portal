@default = "mouseUp"
definition {

	function mouseUp() {
		WaitForSPARefresh();

		selenium.mouseUp(${locator1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}