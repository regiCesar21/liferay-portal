@default = "isNotChecked"
definition {

	function isNotChecked() {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.waitForVisible(${locator1}, "true");

		selenium.isNotChecked(${locator1});
	}

	function isNotCheckedNotVisible() {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "true");

		selenium.isNotChecked(${locator1});
	}

}