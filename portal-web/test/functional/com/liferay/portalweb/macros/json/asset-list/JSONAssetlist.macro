definition {

	@summary = "Default summary"
	macro addDynamicAssetListEntry(groupName = null, site = null, title = null) {
		Variables.assertDefined(parameterList = "${groupName},${title}");

		var groupId = JSONAssetlistSetter.setGroupId(
			groupName = ${groupName},
			site = ${site});
		var type = JSONAssetlistSetter.setType(selectionType = "dynamic");

		JSONAssetlistAPI._addAssetListEntry(
			groupId = ${groupId},
			title = ${title},
			type = ${type});
	}

	@summary = "Default summary"
	macro addDynamicAssetListEntryWithItemType(groupName = null, itemType = null, site = null, title = null) {
		Variables.assertDefined(parameterList = "${groupName},${itemType},${title}");

		var groupId = JSONAssetlistSetter.setGroupId(
			groupName = ${groupName},
			site = ${site});

		JSONAssetlistAPI._addDynamicAssetListEntry(
			groupId = ${groupId},
			itemType = ${itemType},
			title = ${title});
	}

	@summary = "Default summary"
	macro addManualAssetListEntry(groupName = null, site = null, title = null) {
		Variables.assertDefined(parameterList = "${groupName},${title}");

		var groupId = JSONAssetlistSetter.setGroupId(
			groupName = ${groupName},
			site = ${site});
		var type = JSONAssetlistSetter.setType(selectionType = "manual");

		JSONAssetlistAPI._addAssetListEntry(
			groupId = ${groupId},
			title = ${title},
			type = ${type});
	}

}