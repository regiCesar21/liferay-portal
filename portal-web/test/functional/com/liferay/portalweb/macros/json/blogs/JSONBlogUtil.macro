definition {

	@summary = "Default summary"
	macro _convertToXML(content = null) {
		Variables.assertDefined(parameterList = ${content});

		var contentXML = '''<p>${content}</p> ''';

		return ${contentXML};
	}

}